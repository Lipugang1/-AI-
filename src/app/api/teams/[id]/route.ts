import { NextRequest, NextResponse } from "next/server"; import { query, getConnection } from "@/lib/db";
export async function GET(_: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const [r] = await query("SELECT * FROM teams WHERE id=?", [id]) as any[]; return NextResponse.json({ team: r || null }); }
export async function PATCH(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const body = await request.json(); if (body.name) await query("UPDATE teams SET name=? WHERE id=?", [body.name, id]); const [r] = await query("SELECT * FROM teams WHERE id=?", [id]) as any[]; return NextResponse.json({ team: r }); }
export async function DELETE(_: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const conn = await getConnection(); try { await conn.execute("DELETE FROM team_members WHERE team_id=?", [id]); await conn.execute("DELETE FROM teams WHERE id=?", [id]); } finally { await conn.end(); } return NextResponse.json({ success: true }); }
